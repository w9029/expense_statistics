services:
  expense-backend:
    image: alpine:3.22
    container_name: expense-backend
    working_dir: /app
    command: ["/bin/sh", "/app/start_backend.sh"]
    ports:
      - "8090:8090"
    volumes:
      - ./release:/app
    restart: unless-stopped
    environment:
      TZ: Asia/Tokyo

